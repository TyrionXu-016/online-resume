import { notImplemented } from "@/lib/errors";

export async function GET() {
  return notImplemented("GET /api/resumes");
}

export async function POST() {
  return notImplemented("POST /api/resumes");
}
